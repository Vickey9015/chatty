{
  "name": "lockychat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=18 <=24"
  },
  "scripts": {
    "install:client": "npm install --prefix client --include=dev",
    "install:server": "npm install --prefix server",
    "postinstall": "npm run install:server && npm run install:client",
    "prebuild": "npm run install:server && npm run install:client",
    "predev": "node scripts/free-ports.js",
    "dev": "concurrently -k \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm run install:server && npm run install:client",
    "build": "npm run build --prefix client && node scripts/copy-public.js",
    "start": "node index.js",
    "prod": "npm run build && npm run start",
    "deploy:build": "npm run install:all && npm run build",
    "deploy": "npm run deploy:build && npx pm2 start ecosystem.config.cjs --update-env || npx pm2 restart lockychat",
    "deploy:stop": "npx pm2 stop lockychat",
    "deploy:logs": "npx pm2 logs lockychat"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^2.0.0",
    "socket.io": "^4.8.1",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "pm2": "^5.4.3"
  }
}
